# Constraints

## Security

- Secrets handling: `.env` file with `NC_URL`, `NC_USER`, `NC_APP_PASSWORD`. Never logged or embedded in responses.

## Privacy & Data Handling

- PII policy: Board/card content may contain personal data. Do not log request/response payloads.
